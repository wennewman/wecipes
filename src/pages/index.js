import React from 'react'
import Footer from '../components/footer';
import Header from '../components/header';

const IndexPage = () => {
  
  return (
  <div>
    <Header/>
    <h1>Hello.</h1>
    <p>These are the recipes I have collected from my mum and others over the years.  My book is falling apart with loose leaf recipes falling out.  I'm always sharing recipes, so now I can just direct to this site instead of taking </p>
    <Footer/>
  </div>
  )
}

export default IndexPage